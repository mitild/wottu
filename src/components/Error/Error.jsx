import { ErrorContainer, ErrorTitle } from "./Styles"

const Error = () => {
  return (
    <ErrorContainer>
      <ErrorTitle>Ops wrong turn...</ErrorTitle>
    </ErrorContainer>
  )
}

export default Error